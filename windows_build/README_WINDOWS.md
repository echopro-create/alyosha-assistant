# Alyosha Voice Assistant (Windows Version)

## 📋 Требования
1. **Python 3.12+** (с галочкой "Add Python to PATH" при установке)
2. **FFmpeg** (для работы аудио)
   - Скачать с [ffmpeg.org](https://ffmpeg.org/download.html)
   - Добавить `bin` папку в переменные среды PATH (чтобы команда `ffplay` работала в терминале)

## 🚀 Установка

1. **Установите зависимости:**
   Откройте терминал в этой папке и выполните:
   ```cmd
   pip install -r requirements.txt
   ```

2. **Настройте API ключи:**
   - Создайте файл `.env` (скопируйте из `.env.example` если есть, или создайте новый)
   - Добавьте ключи:
     ```ini
     GEMINI_API_KEY=ваш_ключ_gemini
     ELEVENLABS_API_KEY=ваш_ключ_elevenlabs (опционально)
     ```

3. **Скачайте модели:**
   - Скачайте Vosk модель [vosk-model-small-ru-0.22](https://alphacephei.com/vosk/models)
   - Распакуйте в папку `models/vosk-model-small-ru-0.22`

## ▶️ Запуск
Просто запустите файл `run.bat` или выполните в терминале:
```cmd
python main.py
```

## 🧩 Особенности Windows версии
- **Аудио:** Используется `pycaw` для управления громкостью приложений.
- **Уведомления:** Используются нативные Windows уведомления.
- **Пути:** Логи и данные хранятся в `%APPDATA%\Alyosha`.
- **Консоль:** Поддерживает выполнение команд CMD и PowerShell.

## ⚠️ Известные нюансы
- Для корректного отображения кириллицы в консоли используется CP866/UTF-8.
- Некоторые команды Linux (`sudo`, `apt`) не будут работать, используйте аналоги (`winget`, `shutdown`).
